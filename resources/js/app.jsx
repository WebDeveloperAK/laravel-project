import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
    return <h1>Hello, Vite + React + Laravel!</h1>;
}

ReactDOM.createRoot(document.getElementById('app')).render(<App />);
